let _ = require('lodash');
//high function
//global
describe('array-native & lodash', () => {
    it('allRGBA', () => {
        // let emissions = _.range(0, 255).map(num => '' + num);
        let rgbas = [];

        // for (let r = 0; r <= 255; r++) {
        //     for (let g = 0; g <= 25; g++) {
        //         for (let b = 0; b <= 25; b++) {
        //             // for (let a = 0; a <= 1; a = a + 0.5) {
        //             rgbas.push(`rgba${r},${g},${b},1`);
        //             // }
        //         }
        //     }
        // }
        // console.log(rgbas.length);
    });
});
